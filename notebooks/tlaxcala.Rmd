---
title: "Detección de irregularidades en el Padrón Único de Beneficiarios"
output:
  html_document:
    df_print: paged
---
# Introducción
Este reporte presenta avances al análisis de detección de irregularidades en el Padrón Único de beneficiarios.

# Limitaciones: 
- Los resultados de este reporte dependen en buena medida de la identificación única de los beneficiarios. Éste proceso, que terminará en la generación de un nuevo id único por beneficiario, está siendo desarrollado dentro del SISI y se espera su término a mediados de Agosto. Por esta razón, pueden existir duplicidades (beneficiarios contados como dos personas) lo que puede inflar el porcentaje de cobertura. 

```{r, echo=FALSE, results='hide', include=FALSE}
dotenv::load_dot_env("../.env")
library(dbrsocial)
```

```{r, echo=FALSE, results='hide', include=FALSE}
library(ggplot2)
library(tidyverse)
library(plyr)
library(dplyr)
library(scales)
library(maptools)
library(rgdal)
library(ggmap)
library(gridExtra)
library(rgdal)
library(Hmisc)
library(rgeos)
library(sp)
library(sf)
library(rgeos)
library(broom)
library(scales)
library(rangeMapper)
library(ggmap)
library(plotly)
library(viridis)
```

```{r, echo=FALSE, results='hide', include=FALSE, message=FALSE, warning=FALSE}

### DB conections
con <- pub_connect(s3dir = Sys.getenv("S3_DIR"), schema = Sys.getenv("SCHEMA"))
con1 <- prev_connect()
```

```{r, echo=FALSE, results='hide', include=FALSE, message=FALSE, warning=FALSE}
### Dejamos esta función porque por alguna razón no se carga al instalar el paquete ):
load_query <- function(connection,schema,the_table,columns="*",options=""){
    the_query <- "SELECT %s FROM %s.%s"
    complete <- paste0(the_query," ",options)
    schema    <- deparse(substitute(schema))
    the_table <- deparse(substitute(the_table))
    initial <- RPostgreSQL::dbSendQuery(connection,
                             sprintf(complete,columns,schema,the_table))
}

load_geom <- function(connection,schema,the_table,columns="cve_mun, cve_ent, cve_muni, ", geom_col, col_shape, options=""){
    geom_col <- deparse(substitute(geom_col))
    schema    <- deparse(substitute(schema))
    the_table <- deparse(substitute(the_table))
    col_shape <- deparse(substitute(col_shape))

    the_query <- "SELECT %s FROM %s.%s"
    geom_col_as <- sprintf("ST_AsText(%s) as geom",geom_col)
    columns <- paste0(columns,geom_col_as)
    complete <- paste0(the_query," ",options)

    initial <- RPostgreSQL::dbSendQuery(connection,
                             sprintf(complete,columns,schema,the_table)) %>%
    retrieve_result()

    mun_shp = WKT2SpatialPolygonsDataFrame(initial, geom = geom_col, id = col_shape)
    mun_df <- fortify(mun_shp, region = col_shape)
    names(mun_df)[names(mun_df)=="id"] <- col_shape

    return(mun_df)
}

theme_pub <- function(base_size=12, font=NA){
  txt <- element_text(size = base_size+2, colour = "black", face = "plain")
  bold_txt <- element_text(size = base_size+2, colour = "black", face = "bold")
  
  theme_classic(base_size = base_size, base_family = font) +
    
  theme(plot.title = element_text(size = 30, face = "bold"),
        axis.title.x = element_text(size=20),
        axis.title.y = element_text(size=20),
        axis.text.x = element_text(angle = 45, size=18, hjust = 1),
        axis.text.y = element_text(size=22))
}

quant_labels <- function(variable, no_classes=6){
  quantiles <- quantile(variable, 
                        probs = seq(0, 1, length.out = no_classes + 1),na.rm = TRUE)
  labels <- c()
  for(idx in 1:length(quantiles)){labels <- c(labels, paste0(round(quantiles[idx], 2)," – ", round(quantiles[idx + 1], 2))) }
  labels <- labels[1:length(labels)-1]
  variable_q <- cut(variable, breaks = quantiles,labels = labels, include.lowest = T)
  return(variable_q)
}
```


```{r, echo=FALSE, message=FALSE, warning=FALSE}
### Catálogo de queries y la información por municipios de CONEVAL
los_queries <- query_dic()
coneval <- load_table(con1,clean,coneval_municipios) %>% retrieve_result() %>% 
  filter(data_date == "2015-a")
```



# Análisis Nacional


## Número total de beneficiarios y monto total de beneficios monetarios durante el 2017.

```{r, echo=FALSE, message=FALSE, warning=FALSE}
query <- "SELECT count(distinct newid), sum(nuimpmonetario) as monto
              FROM athena_pub.pub_nominal
              WHERE anio=2017"
              
c(monto_anual,los_queries) := load_or_run(con,query,los_queries)
colnames(monto_anual) <- c("Número de Beneficiarios Únicos","Suma de Importes monetarios")
monto_anual
```


## Análisis de Progresividad de la política pública por municipio - Beneficios otorgados durante el 2017

```{r, echo=FALSE, message=FALSE, warning=FALSE}
query <- "SELECT cvemuni, sum(nuimpmonetario) as monto, count(distinct newid) as beneficiarios
              FROM athena_pub.pub_nominal
              WHERE anio=2017
              GROUP BY cvemuni"

c(dinero_mun,los_queries) := load_or_run(con,query,los_queries)
colnames(dinero_mun) <- c("cve_muni","cuenta", "num_beneficiarios")

dinero_mun_ <- dinero_mun %>% 
  left_join(coneval) %>% 
  mutate(ben_pobla=cuenta/pob_tot, 
         ben_pobr=cuenta/pobreza_num, 
         prom_monto = cuenta/num_beneficiarios,
         ben_entre_pob_total = num_beneficiarios/pob_tot,
         ben_entre_pobreza = num_beneficiarios/pobreza_num) %>% 
  arrange(cve_muni)

geom_nal <- load_geom(con1,raw,geom_municipios,geom_col=geom,col_shape=cve_muni)
municipios_nal <- geom_nal %>%
  left_join(dinero_mun_)


dinero_mun_ %>% select(ent,cve_muni, municipio, prom_monto, ben_pobla, ben_pobr, ben_entre_pob_total, ben_entre_pobreza)

```


## Análisis de Beneficios Monetarios

```{r,echo=FALSE, message=FALSE, warning=FALSE}
### Suma de beneficios monetarios por municipio. 
municipios_nal$cuenta_q <- quant_labels(municipios_nal$cuenta)
ggplot() + 
  geom_polygon(data = municipios_nal, aes(long, lat, group=group, fill=cuenta_q))+
  labs(title="Suma de beneficios monetarios por municipio", fill="monto")+
  scale_fill_brewer(na.value="grey") + 
  coord_fixed()
```


```{r,echo=FALSE}
### Promedio de beneficios monetarios por población total en el municipio
municipios_nal$ben_pobla_q <- quant_labels(municipios_nal$ben_pobla)
ggplot() + 
  geom_polygon(data = municipios_nal, aes(long, lat, group=group, fill=ben_pobla_q)) +
  labs(title="Promedio de beneficios otorgados por población total en el municipio", fill="monto") +
  scale_fill_brewer(na.value="grey") + 
  coord_fixed()
```



```{r,echo=FALSE}
### Promedio de beneficios monetarios por número de beneficiarios en el municipio
municipios_nal$prom_monto_q <- quant_labels(municipios_nal$prom_monto)
ggplot() + 
  geom_polygon(data = municipios_nal, aes(long, lat, group=group, fill=prom_monto_q)) +
  labs(title="Promedio de beneficios otorgados por número de beneficiarios en el municipio", fill="monto") +
  scale_fill_brewer(na.value="grey") + 
  coord_fixed()

```



```{r,echo=FALSE}
### Promedio de beneficios monetarios por número de personas en situación de pobreza en el municipio
municipios_nal$ben_pobr_q <- quant_labels(municipios_nal$ben_pobr)
ggplot() + 
  geom_polygon(data = municipios_nal, aes(long, lat, group=group, fill=ben_pobr_q))+
  labs(title="Promedio de beneficios monetarios por personas en situación de pobreza por municipio", fill="monto")+
  scale_fill_brewer(palette="Spectral", na.value="grey") + 
  coord_fixed()

```

## Análisis de cobertura - (número de beneficiarios)

```{r,echo=FALSE,message=FALSE, warning=FALSE}
### Número de beneficiarios por municipio. 
municipios_nal$num_beneficiarios_q <- quant_labels(municipios_nal$num_beneficiarios)
ggplot() + 
  geom_polygon(data = municipios_nal, aes(long, lat, group=group, fill=num_beneficiarios_q))+
  labs(title="Número de beneficiarios por municipio", fill="Número de Beneficiarios")+
  scale_fill_brewer(na.value="grey") + 
  coord_fixed()
```



```{r,echo=FALSE,message=FALSE, warning=FALSE}
### Número de beneficiarios entre la población total del municipio
municipios_nal$ben_entre_pob_total_q <- quant_labels(municipios_nal$ben_entre_pob_total)
ggplot() + 
  geom_polygon(data = municipios_nal, aes(long, lat, group=group, fill=ben_entre_pob_total_q)) +
  labs(title="Tasa de Beneficiarios por población total en el municipio", fill="Tasa de Cobertura") +
  scale_fill_brewer(na.value="grey") + 
  coord_fixed()
```


```{r,echo=FALSE,message=FALSE, warning=FALSE}
### Número de beneficiarios entre población en situación de pobreza en el municipio
municipios_nal$ben_entre_pobreza_q <- quant_labels(municipios_nal$ben_entre_pobreza)
ggplot() + 
  geom_polygon(data = municipios_nal, aes(long, lat, group=group, fill=ben_entre_pobreza_q)) +
  labs(title="Tasade beneficiarios por población en situación de pobreza en el municipio", fill="Tasa de Cobertura") +
  scale_fill_brewer(palette = "Spectral",na.value="grey") + 
  coord_fixed()

```


# Caso de Estudio - Tlaxcala

## Cargamos las geometrías de Tlaxcala
```{r}
options <- "WHERE cve_ent = '29'"
geom_tlax <- load_geom(con1,raw,geom_municipios,geom_col=geom,col_shape=cve_muni,options=options)
```
 
 
## Creamos una tabla de CONEVAL con sólo la información de Tlaxcala

```{r}
coneval_tlaxcala <- coneval[(coneval$ent == "29" & coneval$data_date == "2015-a"),]
```

## Análisis de Beneficiarios
### Revisamos el número de beneficiarios en Tlaxcala por municipio en 2017
```{r}
query <- "SELECT cvemuni, count(distinct newid) as cuenta
          FROM athena_pub.pub_nominal
          WHERE cveent='29' AND anio=2017
          GROUP BY cvemuni
          ORDER BY cuenta DESC"

c(ben_tlax,los_queries) := load_or_run(con,query,los_queries)
colnames(ben_tlax) <- c("cve_muni","cuenta")
ben_tlax$cve_muni <- as.character(ben_tlax$cve_muni)
cuentas <- ben_tlax
cuentas
```

```{r, echo=FALSE, results='hide', message=FALSE, warning=FALSE}
los_estados <- read_csv("../estados.csv")
colnames(los_estados) <- c("num","id","pagos","distintos")
```

### Graficamos el número de beneficiarios por municipio
```{r,echo=FALSE,fig.width=25,fig.height=7}
municipios_tlax <- coneval_tlaxcala %>%
  left_join(cuentas)

ggplot(data=municipios_tlax, aes(x=municipio, y=cuenta)) +
  geom_bar(stat="identity") +
  labs(title="Número de beneficiarios por municipio", fill="Número de beneficiarios",y="Beneficiarios únicos") +
  scale_y_continuous(labels = comma,breaks = pretty_breaks(n = 6)) +
  theme_pub()

```

```{r,echo=FALSE,fig.width=20,fig.height=12}
edos <- geom_tlax %>%
  left_join(cuentas)

ggplot() + 
  geom_polygon(data = edos, aes(long, lat, group=group, fill=cuenta))+
  labs(title="Número de beneficiarios por municipio", fill="Número de beneficiarios")+
  coord_map(projection="mercator") +
  scale_fill_continuous(labels = comma, breaks = pretty_breaks(n = 6)) +
  theme_pub()  +
  coord_fixed()


```

### Revisamos el número de beneficiarios por habitantes y habitantes en pobreza
#### Por población total
```{r,echo=FALSE,fig.width=25,fig.height=7}
municipios_tlax <- mutate(municipios_tlax,ben_pobla=cuenta/pob_tot) %>%
  mutate(ben_pobr=cuenta/pobreza_num)


ggplot(data=municipios_tlax, aes(x=municipio, y=ben_pobla)) +
  geom_bar(stat="identity") +
  labs(title="Número de beneficiarios por habitantes en cada municipio", fill="Número de beneficiarios por habitantes",y="Beneficiarios únicos por habitantes") +
  scale_y_continuous(labels = comma,breaks = pretty_breaks(n = 6)) +
  theme_pub()  
```

```{r,echo=FALSE,fig.width=20,fig.height=12}
edos <- geom_tlax %>%
  left_join(municipios_tlax)

ggplot() + 
  geom_polygon(data = edos, aes(long, lat, group=group, fill=ben_pobla))+
  labs(title="Número de beneficiarios por habitantes en cada municipio", fill="Número de beneficiarios por habitantes")+
  coord_map(projection="mercator") +
  theme_pub() +
  scale_fill_continuous(labels = comma,breaks = pretty_breaks(n = 10)) +
  coord_fixed()
```

#### Por población en pobreza
```{r,echo=FALSE,fig.width=25,fig.height=7}
ggplot(data=municipios_tlax, aes(x=municipio, y=ben_pobr)) +
  geom_bar(stat="identity") +
  labs(title="Número de beneficiarios por habitantes en pobreza por municipio", fill="Número de beneficiarios por habitantes en pobreza",y="Beneficiarios únicos por habitantes en pobreza") +
  scale_y_continuous(labels = comma,breaks = pretty_breaks(n = 6)) +
  theme_pub() 
```



```{r,echo=FALSE,fig.width=20,fig.height=12}
ggplot() + 
  geom_polygon(data = edos, aes(long, lat, group=group, fill=ben_pobr))+
  labs(title="Número de beneficiarios por habitantes en pobreza por municipio", fill="Número de beneficiarios por habitantes en pobreza",y="Beneficiarios únicos por habitantes en pobreza") +
  coord_map(projection="mercator") +
  scale_fill_continuous(labels = comma,breaks = pretty_breaks(n = 8)) +
  theme_pub() 
  coord_fixed()
```



### Distribución de pago por programa
```{r,echo=FALSE,results='hide'}
query <- "SELECT cdprograma, nuimpmonetario
              FROM athena_pub.pub_nominal
              WHERE cveent='29'"
              
c(dinero_programa,los_queries) := load_or_run(con,query,los_queries)
dinero_programa
```

```{r,echo=FALSE,fig.width=25,fig.height=7}
box_programa <- ggplot(dinero_programa, aes(x = cdprograma, y = nuimpmonetario, fill=cdprograma)) +
        geom_boxplot() +
        labs(title="Distribución de pagos", fill="Número de beneficiarios por habitantes en pobreza",y="Beneficiarios únicos por habitantes en pobreza") +
  theme_pub() +
        scale_y_log10()
box_programa
```


### Distribución de pago por programa mensual
```{r,echo=FALSE,results='hide'}
query <- "SELECT anio, numespago, cdprograma, sum(nuimpmonetario) as monto, newid
              FROM athena_pub.pub_nominal
              WHERE cveent='29'
              GROUP BY anio, newid, numespago, cdprograma"
              
c(dinero_programa_mensual,los_queries) := load_or_run(con,query,los_queries)
dinero_programa_mensual
```


```{r,echo=FALSE,fig.width=25,fig.height=7}
box_programa_mensual <- ggplot(dinero_programa_mensual, aes(x = cdprograma, y = monto, fill=cdprograma)) +
        geom_boxplot() +
        theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
  theme_pub() +
        scale_y_log10()
box_programa_mensual
```


### Distribución de pago por programa anual

```{r,echo=FALSE,results='hide'}
query <- "SELECT anio, cdprograma, sum(nuimpmonetario) as monto, newid
              FROM athena_pub.pub_nominal
              WHERE cveent='29'
              GROUP BY anio, cdprograma, newid"
              
c(dinero_programa_anual,los_queries) := load_or_run(con,query,los_queries)
dinero_programa_anual
```


```{r,echo=FALSE,fig.width=25,fig.height=7}
box_programa_anual <- ggplot(dinero_programa_anual, aes(x = cdprograma, y = monto, fill=cdprograma)) +
        geom_boxplot() +
  theme_pub() +
        scale_y_log10()
box_programa_anual
```

### Contar el número de distintos newid, ver cuánto se les paga en total con municipio
```{r,results='hide'}
query <- "SELECT cvemuni, count(distinct newid) as iden, cdprograma, sum(nuimpmonetario) as monto, cdbeneficio
              FROM athena_pub.pub_nominal
              WHERE cveent='29'
              GROUP BY cdprograma, cdbeneficio, cvemuni"

              
c(num_per,los_queries) := load_or_run(con,query,los_queries)
```

### Suma de pagos por distintas personas
```{r,echo=FALSE}
query <- "SELECT cvemuni, count(distinct newid) as iden, sum(nuimpmonetario) as monto
              FROM athena_pub.pub_nominal
              WHERE cveent='29'
              GROUP BY cvemuni"

c(pago_por_persona,los_queries) := load_or_run(con,query,los_queries)
pago_por_persona <- mutate(pago_por_persona,ratio=monto/iden)
```


```{r,echo=FALSE,fig.width=20,fig.height=12}
colnames(pago_por_persona)[which(names(pago_por_persona) == "cvemuni")] <- "cve_muni"
pago_por_persona$cve_muni <- as.character(pago_por_persona$cve_muni)

edos <- geom_tlax %>%
  left_join(pago_por_persona)

ggplot() + 
  geom_polygon(data = edos, aes(long, lat, group=group, fill=ratio))+
  labs(title="Suma de montos pagados por municipio", fill="monto")+
  theme_pub() +
  coord_map(projection="mercator") +
  coord_fixed()
```





